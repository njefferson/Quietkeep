{
  "name": "quietkeep",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Quietkeep — a free, local-first, event-sourced planner. Static PWA, no accounts, no server.",
  "license": "SEE LICENSE IN LICENSE.md",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run test:only",
    "test:only": "node --test --experimental-strip-types 'test/**/*.test.ts'",
    "brand": "node tools/brand.mjs",
    "brand:check": "node tools/brand.mjs --check",
    "build": "esbuild src/ui/entry.ts --bundle --format=esm --target=es2022 --outfile=public/app.js --sourcemap --log-level=warning",
    "serve": "node tools/serve.mjs",
    "smoke": "node --experimental-strip-types tools/smoke.mjs",
    "update:walk": "node --experimental-strip-types tools/update-walk.mjs",
    "changelog": "node --experimental-strip-types tools/changelog.mjs",
    "changelog:check": "node --experimental-strip-types tools/changelog.mjs --check",
    "a11y": "node tools/a11y.mjs",
    "look": "node tools/look.mjs",
    "tour:shots": "node tools/tour-shots.mjs",
    "tour:check": "node tools/tour-shots.mjs --check",
    "gates:audit": "node tools/gate-audit.mjs",
    "headers:check": "node tools/headers.mjs",
    "build:sync": "node tools/editions.mjs",
    "editions:check": "node tools/editions.mjs --check",
    "workflows:check": "node tools/workflows.mjs",
    "vocabulary": "if grep -rniE '\\b(overdue|streak)\\b' src/ | grep -viE 'no |never|banned|forbid|law 5'; then echo 'banned vocabulary found in src/ — see ADR-0010' >&2; exit 1; fi; if git ls-files | xargs grep -niE 'getting things done|\\bGTD\\b' 2>/dev/null | grep -viE 'never|banned|forbid|marks|copyrighted|ciphertext|base64|produced'; then echo 'the GTD marks are banned outright (CLAUDE.md) — name the practice, not the mark' >&2; exit 1; fi; echo clean",
    "storage:check": "if grep -rnE '\\b(localStorage|sessionStorage)\\b' src/ public/sw.js public/index.html | grep -viE 'banned|never|no localStorage|not localStorage'; then echo 'localStorage/sessionStorage found — storage is IndexedDB via Dexie, and the ban is outright' >&2; exit 1; fi; echo clean",
    "thesis": "node tools/thesis.mjs",
    "thesis:check": "node tools/thesis.mjs --check",
    "events:check": "node --experimental-strip-types tools/event-list.mjs",
    "writegate:check": "node tools/write-gate.mjs",
    "emitters:check": "node --experimental-strip-types tools/emitters.mjs",
    "sample:check": "node --experimental-strip-types tools/sample-coverage.mjs",
    "plain:check": "node tools/plain.mjs",
    "reasons:check": "node tools/reasons.mjs",
    "controls:check": "node tools/controls.mjs",
    "collisions:check": "node tools/collisions.mjs",
    "adr:check": "node tools/adr-index.mjs",
    "touch:check": "node tools/touch-check.mjs",
    "notify:check": "node tools/notify-voice.mjs",
    "quotes:check": "node ../noahjefferson/quote-check.mjs --repo .",
    "questions:check": "node tools/questions.mjs",
    "release:check": "node --experimental-strip-types tools/release-check.mjs",
    "size:check": "node tools/size-check.mjs",
    "prepare": "node ../noahjefferson/branch-guard.mjs --repo . --install >/dev/null 2>&1 || true",
    "voice:pronouns": "node tools/voice-pronouns.mjs"
  },
  "//build": "esbuild strips types and bundles src/ui into one ES module. It is the ONLY build step and it does no transpiling down — target es2022 is what the reference platform already runs. See ADR-0026 for why a build exists at all in a family that avoids them.",
  "//playwright": "1.56.0 ships chromium revision 1194, which is what /opt/pw-browsers/chromium-1194 is. These are a MATCHED PAIR — see the hub's LESSONS.md section 8. Bumping one without the other silently breaks every render with no useful error.",
  "dependencies": {
    "dexie": "4.0.11"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "axe-core": "4.10.2",
    "esbuild": "0.25.10",
    "playwright-core": "1.56.0",
    "typescript": "5.7.3"
  },
  "//hooks": "`prepare` runs on every `npm ci` — the one command every session and every CI job already runs — and installs the branch guard the hub generates from .branch-guard. It writes into .git/hooks, which no branch owns: the first attempt pointed core.hooksPath at the tracked directory and failed OPEN, because checking out an older branch deletes the hook with it, and the branch most needing protection is the one most likely to be older."
}
